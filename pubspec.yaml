name: my_clean_app
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  health_connector: ^2.4.0          # پکیج جدید برای دیتای سلامت

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
